#   "AWS CodeDeploy User Guide" at http://docs.aws.amazon.com/codedeploy/latest/userguide/app-spec-ref.html
version: 0.0
os: linux
files:
  - source: /
    destination: /home/ec2-user/mag-web-app

permissions:
  - object: /
    pattern: "**"
    owner: ec2-user
    group: ec2-user

hooks:
  ApplicationStop:
    - location: deploy/app-stop
      timeout: 10
      runas: ec2-user

  AfterInstall:
    - location: deploy/install-pkgs
      timeout: 1200
      runas: ec2-user

  ApplicationStart:
    - location: deploy/app-start
      timeout: 60
      runas: ec2-user

  ValidateService:
    - location: deploy/app-validate
      timeout: 10
      runas: ec2-user
